{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug ROM Builder",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/debug/rom-builder.exe",
      "args": [],
      "cwd": "${workspaceFolder}",
      "preLaunchTask": "cargo build"
    }
  ]
}
